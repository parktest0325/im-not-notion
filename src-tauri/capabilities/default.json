{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-open",
      "allow": [
        { "url": "https://*/*" },
        { "url": "http://*/*" }
      ]
    },
    "dialog:default"
  ]
}
